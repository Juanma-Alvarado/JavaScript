// Valor Numerico
var edad = 11
edad += 1

// Inicializar Y Declararla
var peso = 48
peso -= 2

// Codición (Si Come Sandwich Peso + 1)
var sandwich = 1
peso += sandwich

// Condición (Si juga Futbol peso - 3)
var jugarFutbol = 3
peso -= jugarFutbol

// Programa (Mercado)
var precioDeManzana = 200.3
var total = Math.round(precioDeManzana * 100 * 3) / 100

// Para Decir Cuantos Decimales Queremos
var totalStr = total.toFixed(2)

// Cambiar String a Decimal
var total2 = parseFloat(totalStr)

//Programa  (Porciones)
var pizza = 8
var personas = 2
var catidadDePorCionesPorPersonas = pizza / personas